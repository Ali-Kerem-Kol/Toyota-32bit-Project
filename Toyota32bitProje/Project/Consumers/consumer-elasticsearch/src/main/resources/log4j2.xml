<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>

        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %c{1} - %msg%n"/>
        </Console>

        <!-- RollingFile Appender -->
        <RollingFile
                name="RollingFileLog"
                fileName="logs/consumer-elastic.log"
                filePattern="logs/consumer-elastic-%d{yyyy-MM-dd}-%i.log.gz"
                append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] [%t] %c{1} - %msg%n"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="7"/>
        </RollingFile>

    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFileLog"/>
        </Root>

        <!-- Opsiyonel: Belirli pakete debug vermek istersen -->
        <!--
        <Logger name="com.mydomain.consumer" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFileLog"/>
        </Logger>
        -->
    </Loggers>
</Configuration>
